
-- 1=1 is for?
-- The result is always 'TRUE'
-- It's useful, because you can add more conditions below freely and easy to make them become comment
WHERE 1=1
AND yymm = '202101'
AND gender = 'male'
-- AND nation = 'Korea'

-- 1=2 is for?
-- The result is always 'FALSE'
-- It's useful when creating table
WHERE 1=2

